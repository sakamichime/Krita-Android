# Krita 开发操作手册

## 环境总览

```
宿主机                                    Docker 容器 (krita-auto-1)
──────                                    ────────────────────────
/home/sakamichi/项目/Krita-csp/
├── CMakeLists.txt          ──挂载──>  /home/sakamichi/项目/Krita-csp/    (源码)
├── libs/                                  ▲
├── plugins/                               │ symlink
├── build/krita-build/      ──挂载──>  ~/appimage-workspace/krita-build/ (build)
│   └── bin/krita                         
│
./krita-auto-1/persistent/
├── deps/                   ──挂载──>  ~/persistent/deps/                (依赖)
└── krita → (symlink)       ──挂载──>  ~/persistent/krita/              (指向源码)
```

## 修改源码

源码位置：`/home/sakamichi/项目/Krita-csp/`

推荐编辑器（宿主机上使用）：
- **VS Code**: `code /home/sakamichi/项目/Krita-csp/`
- **Qt Creator**: 官方推荐，`./bin/qtcreator` 启动（容器环境感知）
- **Vim / Neovim**: 终端编辑器
- **任何其他编辑器**

修改后保存即可，容器内自动可见。

---

## 编译（核心操作）

### 完整编译（首次或 cmake 改了）

```bash
# 进入容器
cd /mnt/文档/Users/sakamichi/Projects/krita-auto-1
./bin/enter

# 容器内操作
cd ~/appimage-workspace/krita-build
run_cmake.sh ~/persistent/krita   # 配置 cmake（仅首次需要）
make -j$(nproc) install           # 编译并安装
```

### 增量编译（修改少量源码后）

```bash
./bin/enter
cd ~/appimage-workspace/krita-build && make -j$(nproc) install
```

`make` 会自动检测改动的文件，只重新编译必要的部分，通常几分钟内完成。

### 从宿主机一键编译

```bash
cd /mnt/文档/Users/sakamichi/Projects/krita-auto-1
./bin/build-krita
```

---

## 运行 Krita

```bash
cd /mnt/文档/Users/sakamichi/Projects/krita-auto-1
./bin/enter
krita
```

如果只需要测试改动的效果，不需要每次都重新编译，只运行即可。

---

## 编译产物在哪

| 文件 | 容器内路径 | 宿主机路径 |
|---|---|---|
| krita 主程序 | `~/appimage-workspace/krita-build/bin/krita` | `/home/sakamichi/项目/Krita-csp/build/krita-build/bin/krita` |
| 共享库 (.so) | `~/appimage-workspace/krita-build/lib/` | `/home/sakamichi/项目/Krita-csp/build/krita-build/lib/` |
| 插件 (.so) | `~/appimage-workspace/krita-build/bin/*.so` | `/home/sakamichi/项目/Krita-csp/build/krita-build/bin/*.so` |
| 安装后主程序 | `~/appimage-workspace/krita.appdir/usr/bin/krita` | 容器内（不挂载） |

---

## 容器管理

```bash
# 进入容器
cd /mnt/文档/Users/sakamichi/Projects/krita-auto-1
./bin/enter

# 退出容器
exit

# 停止容器（不丢失数据）
./bin/stop

# 启动容器
./bin/start

# 完全删除容器（会丢失 build 目录内未同步的内容）
docker rm krita-auto-1
```

---

## 常见操作场景

### 场景 1：修复一个 bug
```bash
# 宿主机: 修改源码
vim /home/sakamichi/项目/Krita-csp/plugins/xxx/xxx.cpp

# 容器: 重新编译
./bin/enter
cd ~/appimage-workspace/krita-build && make -j$(nproc) install

# 容器: 运行测试
krita
```

### 场景 2：新增文件
```bash
# 宿主机: 创建新文件
touch /home/sakamichi/项目/Krita-csp/xxx/new_file.cpp

# 需要重新运行 cmake 让新文件被识别
./bin/enter
cd ~/appimage-workspace/krita-build
run_cmake.sh ~/persistent/krita
make -j$(nproc) install
```

### 场景 3：只想运行不想编译
```bash
./bin/enter
krita
```

---

## 注意事项

1. **修改源码在宿主机**，编译在容器内，宿主机无需安装任何编译工具
2. **`make install` 是必须的**，只 `make` 不 `install` 的话，`krita` 命令找不到新编译的二进制
3. **首次 cmake 只需要一次**，之后除非修改了 `CMakeLists.txt` 或新增/删除文件，不需要重新 cmake
4. **build 目录已挂载到宿主机**，`/home/sakamichi/项目/Krita-csp/build/krita-build/bin/krita` 就是编译好的程序
5. 如果在容器内需要查看或复制文件到宿主机，直接看 `/home/sakamichi/项目/Krita-csp/build/` 即可
